# oxichrome-core

Async Rust bindings to Chrome Extension APIs (storage, tabs, runtime) for use in WebAssembly.

This is an internal crate. You probably want [oxichrome](https://crates.io/crates/oxichrome) instead.
