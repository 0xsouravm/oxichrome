# oxichrome-macros

Proc macros (`#[extension]`, `#[background]`, `#[on]`, `#[popup]`, `#[options_page]`) that generate wasm-bindgen exports for Chrome extensions.

This is an internal crate. You probably want [oxichrome](https://crates.io/crates/oxichrome) instead.
